// Retired. Kept as an empty replacement so copying this update removes the old
// SMTP implementation without requiring a separate file deletion.
// PasswordRecoveryService inserts EmailMessage; the existing external job sends it.
